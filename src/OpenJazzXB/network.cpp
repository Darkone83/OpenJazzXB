/**
 * network.cpp
 * XbJazz port — stub.
 */

#include "network.h"

 /* All network function bodies are inline in network.h for the stub.
	This translation unit just ensures the globals are defined when
	EXTERN expands to nothing (i.e. in main.cpp). */